# Qubilt

A modular open-source business platform.

## Quick Start

```bash
# Install dependencies
pnpm install

# Start dev services (Postgres, Redis, MinIO, Meilisearch)
docker compose -f docker/docker-compose.dev.yml up -d

# Start development
pnpm dev
```

See [docs/](./docs/) for full documentation.
